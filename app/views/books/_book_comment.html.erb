	<% book_comments.each do |book_comment| %>
	  <table>
		<tr>
		  <td><%= attachment_image_tag(book_comment.user, :profile_image, :fill, 100, 100, fallback: "no-image-mini.jpg") %><br>
		  	<%= book_comment.user.name %>
		  </td>
		  <td><%= book_comment.body %></td>
		  <td><%= link_to "Destroy", book_book_comment_path(book_comment.book.id,book_comment.id), method: :delete, class: "btn btn-danger pull-right", remote: true  %></td>
		 </tr>
	  </table>
	<% end %>